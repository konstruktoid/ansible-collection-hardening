---
argument_specs:
  main:
    short_description: "Configure default umask settings for user sessions."
    description:
      - "This role configures the default umask value for user sessions."
      - "This role sets the default umask value for user sessions."
    author:
      - "Thomas Sjögren (@konstruktoid)"
    options:
      umask_value:
        description: "Sets the default umask value."
        type: "str"
        default: "0077"
