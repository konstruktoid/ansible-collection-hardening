---
- name: Reload systemd
  become: true
  ansible.builtin.systemd_service:
    daemon_reload: true
    daemon_reexec: true
  when:
    - ansible_facts.virtualization_type not in ["container", "docker", "podman"]
